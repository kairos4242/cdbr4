///get_build_time(object_to_build)
function get_build_time(argument0) {
	//supposed to return each object's build time but for now will just return 120 aka 2 seconds
	var object_to_build = argument0;
	return 120;


}
